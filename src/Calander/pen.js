import React from 'react'

export const pen = () => {
  //製作 + 按鈕
  // 點選 + 按鈕 新增TodoList的陣列物件儲存在 events / locstorg
  /*[{
    tiitle:'',
    countent:[{},{},{}],
    }]*/
  // 點選 title 跳出TodoList
  //清空行事曆 & locstorg
  //select 新增 event 改成 haver + 按鈕
  return <div>pen</div>
}
